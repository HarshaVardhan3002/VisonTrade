'use client';

// This component's logic has been merged into MobileAssistantDrawer
// to resolve a UI bug where two buttons were rendered in the same spot on mobile.
export function ContextualAssistant() {
  return null;
}
